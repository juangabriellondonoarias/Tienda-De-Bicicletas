spring:
  application:
    name: Bicicletas
  datasource:
    url: jdbc:mysql://localhost:3306/tiendaBicicletas?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: JUAN1003
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# --- Nueva sección para JWT ---
security:
  jwt:
    # Esta es la frase "tienda-bicicletas-secret-key-2026-sena" en Base64
    secret-key: dGllbmRhLWJpY2ljbGV0YXMtc2VjcmV0LWtleS0yMDI2LXNlbmE=
    # Expiración: 3600000 ms = 1 hora
    token-expiration: 3600000