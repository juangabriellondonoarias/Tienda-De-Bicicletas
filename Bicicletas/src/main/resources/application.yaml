spring:
  application:
    name: Bicicletas

  datasource:
    # Ajusta el host y puerto si es necesario; el nombre de la DB debe ser igual al que creaste
    url: jdbc:mysql://products-mysql:3306/tiendaBicicletas?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: juanycarlos        # Tu usuario de MySQL
    password: JuanYCarlos2026 # Tu contraseña de MySQL
    driver-class-name: com.mysql.cj.jdbc.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: juangabriellondonoarias@gmail.com
    password: 'ntqg xfja njib eoqw'
    properties:
      mail.smtp.auth: "true"
      mail.smtp.starttls.enable: "true"
      mail.smtp.connectiontimeout: 5000
      mail.smtp.timeout: 5000
      mail.smtp.writetimeout: 5000

  jpa:
    hibernate:
      # 'update' valida y actualiza la estructura sin borrar tus datos existentes
      ddl-auto: update
    show-sql: true        # Muestra las consultas en consola para depuración
    properties:
      hibernate:
        format_sql: true

security:
  jwt:
    secret-key: dGllbmRhLWJpY2ljbGV0YXMtc2VjcmV0LWtleS0yMDI2LXNlbmE=
    token-expiration: 3600000

app:
  frontend-url: http://localhost:4200
