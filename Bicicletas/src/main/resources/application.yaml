spring:
  application:
    name: Bicicletas

  datasource:
    # Prioriza las variables de Render; si no existen, usa tus credenciales de Railway
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://maglev.proxy.rlwy.net:39735/railway?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:wIICeKCFsCYIPQxjbVmtoigFfRMmzIDB}
    driver-class-name: com.mysql.cj.jdbc.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: juangabriellondonoarias@gmail.com
    # Mantiene tu contraseña de aplicación de Gmail
    password: ${MAIL_PASSWORD:ntqgxfjanjibeoqw}
    properties:
      mail.smtp.auth: "true"
      mail.smtp.starttls.enable: "true"

  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

# CONFIGURACIÓN OBLIGATORIA PARA DESPLIEGUE EN RENDER
server:
  port: ${PORT:8080}

security:
  jwt:
    # Usa tu clave secreta de la tienda
    secret-key: ${JWT_SECRET:dGllbmRhLWJpY2ljbGV0YXMtc2VjcmV0LWtleS0yMDI2LXNlbmE=}
    token-expiration: 3600000

app:
  # Permite que el frontend cambie según el entorno
  frontend-url: ${FRONTEND_URL:http://localhost:4200}